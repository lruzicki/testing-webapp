import React from 'react';
import { Tooltip } from 'react-tooltip';

const CustomTooltip = () => (
  <div className='global-tooltip'>
    <Tooltip id='my-tooltip' className='global-tooltip-xx' />
  </div>
);

export default CustomTooltip;
